{
  "name": "strp2p-proxy",
  "version": "1.0.0",
  "description": "Proxy com Chrome REAL (interface gráfica via xvfb)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-deps": "apt-get update && apt-get install -y xvfb google-chrome-stable"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^24.15.0"
  }
}
